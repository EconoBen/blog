# Wing follow-up review — September 10, 2026

> Status — LOCAL WING FOLLOW-UP COMPLETE, September 10, 2026. Build `bbTrs4g9kljfX6-Ec1gvs` passes all 33 source scripts, types, production build and all 12 release checks. Desktop/phone unfolding frames, independent critique and Chromium/WebKit playback checks pass. The current preview is `http://localhost:3112/`. Nothing was published; release requires explicit approval.

Build `bbTrs4g9kljfX6-Ec1gvs` was prepared with Node 22.14.0 from 889 source files. The source-manifest SHA-256 is `c4c213969db0d52f2dc08819cb3842daaec76a34e6e2eab6d12aee83a8610bfe`. The current retained snapshot is `/var/folders/17/b7kp7zcd0xbc4vr9sm2w848h0000gn/T/econoben-release-zft0N7/source`; its served build manifest was verified byte for byte on port 3112. The prior temporary snapshot/server below no longer exists; its persistent recordings and release record remain historical evidence.

The [wing-revision README](../../../release-artifacts/2026-09-10-grebe-film/wing-revision/README.md) indexes final desktop/phone/WebKit videos, 40 ms adjacent unfolding frames, browser results, exact artwork provenance and release logs. Independent review found a remaining two-band overlap in v2; v3 removes it from the first moving-wing frame. The root inspected the frozen build's desktop and phone sheets plus WebKit unfolding, confirming exposed flank, connected shoulders and an uninterrupted first stroke.

New failing regressions reproduced the actual opening's reversal and remaining painted upper-back/white-band pixels, then passed after correction. All 33 source scripts, whole-source types, production build and 12 local release checks pass. The new fourth character image participates in the preparation barrier. Current browser evidence verifies full completion on desktop and phone, replay and Skip during unfolding, Escape/focus recovery, and readable content with no lock if the new flank asset fails. Chromium and Mac WebKit produced no application page errors. No physical phone or new timing benchmark was run; previous timing measurements are not represented as current evidence.

Current four-character-art download total is 1,536,176 bytes. The resident identity, accepted eyelid, later flight clock and story beat times are preserved. Documentation-only completion updates after the snapshot do not alter the verified application. Archive remains deferred until approved publication/reconciliation.

# Prior accepted candidate — historical review

The following is the retained review record before the wing follow-up. Its references to “current” and “persistent” describe that earlier review, not the new delivered build.

# Current candidate review

> LOCAL REVIEW CANDIDATE COMPLETE, September 10, 2026. The frozen production candidate has passed source, type, build, served and Chromium/WebKit checks. Current critical-frame review is complete. The preview and recordings are provided with the evidence index and limitations. Nothing has been published.

## Candidate identity and release checks

Build `Ez3veJ-FQzSFRk1LqwhFg` comes from `/var/folders/17/b7kp7zcd0xbc4vr9sm2w848h0000gn/T/econoben-release-nc3Nvg/source` and is served for review at `http://localhost:3112/`. Its build manifest was verified byte for byte. The source-manifest SHA-256 is `cc4f59a8ed8843b972e3ac0cfa393f8818d755f3eb3fdd637bd9c9f3da0c5d82`; the base commit `8b8c4485135e78476d1ff5d857b101a67706c82e` alone does not identify the uncommitted candidate.

The [release record](/var/folders/17/b7kp7zcd0xbc4vr9sm2w848h0000gn/T/econoben-release-nc3Nvg/release-record.json) records Node 22.23.2, all 33 source scripts, whole-source types, a production build and all 12 release checks passing. Those checks verify 888 source files stayed unchanged during installation/build, the owned server serves this build, and cache/social/reader/topic/fragment/link behavior. The gate's temporary origin was port 3114; the review preview on port 3112 serves the same prepared bytes.

The [review-candidate README](../../../release-artifacts/2026-09-10-grebe-film/review-candidate/README.md) maps all six story beats and retained safeguards to the actual recordings, adjacent frames, browser JSON and performance samples. It is the current evidence index. Earlier `/tmp` recordings and `release-artifacts/.../final/` files are iteration evidence with different build identities.

## Visual and responsive results

The root reviewed current desktop/phone sequence sheets, 36 adjacent desktop takeoff frames beginning at video offset 3.2 s, 36 adjacent landing frames beginning at 10.7 s and the full-size phone feather-contact image at 7.9 s. Connected wing roots, up/downstrokes, quill contact and continuous resident handoff passed that inspection without a new must-fix finding.

The [independent critique](../../../release-artifacts/2026-09-10-grebe-film/final/independent-critique.md) found no additional blocking anatomy or continuity defect in the same artwork/choreography before the final responsive corrections. It does not claim to review those later changes. Current-build Chromium and [WebKit results](../../../release-artifacts/2026-09-10-grebe-film/review-candidate/webkit-results.json) verify the corrected invitation and measured-header clearance.

The earlier 844×390 rotation review had exposed an invitation below the viewport. A separate replay case also placed the crest beneath a header taller than the original fixed allowance. The invitation now sits beside the bird in short landscape layouts, and camera travel uses the actual header bottom plus 18 px, with a 90 px minimum. In current production rotation evidence the bird's conservative top is y182.02, the invitation y317.05–355.05 and the header bottom y114.94. The 320/390 px results also retain visible actions, “Find an article” focus and no post-contact scrolling. The corrected cases have integration regressions.

The independent WebKit 26.4 pass verified identical served/snapshot manifest bytes and passed fresh landscape, replay from scroll 1136, rotation at 10329 ms, portrait completion, touch Skip and reduced-motion article selection. Landscape replay kept the conservative bird top at y133.01 beneath the header plus 18 px, with its invitation at y268.03–306.03. The inspected rotation/replay captures showed no browser-specific clipping or doubled silhouette. No application page errors occurred; local Vercel analytics 404/MIME messages remain recorded. This was Mac WebKit with viewport/touch emulation, not a physical-iPhone check.

## Access and performance

Current Chromium JSON records native Skip and deadline recovery after delayed hydration, Escape/focus behavior, no automatic repeat, working article selection/link, reduced motion, missing artwork and no-JavaScript access. Those scenarios report no page errors. Successful completion leaves no arrival canvas, cover, inert lock or overflow lock. Source regressions separately cover hidden-time preservation, blocked storage, stale loads, StrictMode, history/navigation restoration, exact handoff geometry, drops/contact and allocation bounds.

Current unrecorded host-Mac Chromium samples retain raw timings and per-beat groups. Launch intervals at the 95th percentile are 17.4 ms on desktop and 16.9 ms in the emulated phone viewport. From shake through settle, neither run records an interval above 33.4 ms; rendered-elapsed change maxima are 18.4/18.0 ms. Each run still records an approximately 101 ms initial setup long task. The 25 fps videos are visual evidence, not proof of rendering performance.

The torso retains an engraved stability while the head, neck and wings move. The highest desktop primary tips briefly crop during the close upward exit. Physical-mobile playback/performance remains unverified. The three character WebPs total 1,128,926 bytes, with the existing shoreline reused; [provenance](artwork-provenance.md) records the opaque RGB plates and native masks accurately.

## Local handoff

The persistent preview at `http://localhost:3112/` and desktop/phone recordings in the review-candidate directory are the delivered candidate. The preview and desktop recording were queued in Codex and both recordings are linked in the final handoff. Owned test browsers and the development server were closed; the verified local production preview remains running. Publishing requires separate user approval.

Strict `openspec validate refine-grebe-arrival-film --strict --no-interactive` passes for the updated records. All tasks are complete with the scoped evidence above. Documentation after the source freeze does not alter the application in the reviewed build.
