# Anatomy and two-pass QA inventory

> Status — IMPLEMENTED AND VERIFIED LOCALLY, September 10, 2026. The isolated anatomy gate and integrated four-viewport artistic review passed. A focused follow-up accepted the resting-body handoff refinement. The complete production gate, native fallback/lifecycle checks and unrecorded performance are recorded for build `QsI2QzDIQ1r87XsztmOSc`. Independent final Chromium/WebKit and replay/rotation/Skip checks pass; the verified preview and concise review guide are delivered by the root task. Startup pauses and minor visual limits are retained, and native visibility is not inferred from source tests.

## Known baseline

The previous local preview was verified before replacement. A read-only request for `/_next/static/bbTrs4g9kljfX6-Ec1gvs/_buildManifest.js` returned HTTP 200 on September 10 and matched the retained snapshot bytes. Its build-manifest SHA-256 is `d826c1113f0fa878aecaa779eb991761f32a41b3fc4c8db125e808b508b4c195`.

| Baseline field | Verified value |
| --- | --- |
| Build | `bbTrs4g9kljfX6-Ec1gvs` |
| Source manifest SHA-256 | `c4c213969db0d52f2dc08819cb3842daaec76a34e6e2eab6d12aee83a8610bfe` |
| Retained source | `/var/folders/17/b7kp7zcd0xbc4vr9sm2w848h0000gn/T/econoben-release-zft0N7/source` |
| Recorded runtime | Node 22.14.0, npm 10.9.2 |
| Historical release record | `release-artifacts/2026-09-10-grebe-film/wing-revision/release-record.json` |
| Prior design and critique | `openspec/changes/refine-grebe-arrival-film/` and the wing-revision evidence directory |

This baseline is retained for comparison and local rollback. Its earlier acceptance and videos remain historical; new anatomy and two-pass acceptance come from the separate development evidence below. Current production evidence appears below and does not replace or rewrite this historical baseline.

## Current verified production candidate

| Field | Verified value |
| --- | --- |
| Local preview | `http://localhost:3112/` |
| Build | `QsI2QzDIQ1r87XsztmOSc` |
| Source manifest SHA-256 | `bb9cb299a1fa46e8aa5c98eb28900b8de26985dba6b75bc32c5b7208eb4d1be7` |
| Retained source | `/var/folders/17/b7kp7zcd0xbc4vr9sm2w848h0000gn/T/econoben-release-1Wilcd/source` |
| Gate | All 39 source scripts, types, locked install, production build and served cache/social/reader/topic/fragment/link checks pass |
| Source preservation | 910 files retained byte for byte by installation/build; final documentation is a later evidence update |
| Served identity | `production-review/preview-identity.json` and independent `identity.json` confirm exact build-manifest bytes |
| Release record | `release-artifacts/2026-09-10-two-pass/release-record.json`; gate logs copied to `production-review/` |

The first gate attempt found an obsolete test of the prior wing rig. That regression was migrated to the actual shared flight rig, and the complete rerun passed. Application/art/test source remained frozen through production review. The root records the documentation-only differences from the snapshot; no new application build is implied by this final prose update.

## Evidence conventions

Current evidence lives under `release-artifacts/2026-09-10-two-pass/`, with separate `artwork`, `anatomy-critique`, `book-review`, `integrated-review`, historical `functional-review` and final `production-review` records. Paths below are relative to that directory unless fully specified. Each recording/check names its build or source/art hashes, browser, viewport/device-pixel ratio, clock landmarks and actual date. Record whether playback was normal speed, a paused frame or a performance run. Reference observations include source links and what each shows; generated artwork records actual originals/prompts, dimensions/alpha, hashes and composition decisions.

Each row below identifies its concrete source/native evidence and any unresolved verification limit. A source test establishes a contract, a recording supports visual judgment, and unrecorded frame intervals support a performance statement. None substitutes for the others. Final source manifest and browser evidence must describe the same candidate; classify later documentation-only changes explicitly.

## Requirement to evidence map

| Requirement / acceptance | Required evidence | Current result |
| --- | --- | --- |
| Authentic anatomy study | Reference links/observations covering shoulder, proportions, elbow/wrist, feather overlap, perspective and body response; current-film critique | Reference inspection complete in `reference-research.md`; observed silhouettes/postures informed the accepted study. Joint coordinates and stroke frequency are design choices, not measured by these references. |
| Isolated study before integration | Normal-speed unfolding plus two complete cycles; adjacent frames; independent critique and recorded gate decision before integration | PASS in `anatomy-critique/final-review.md`. Two normal-speed plays each at 1440×900 and 390×844 plus native adjacent/staged 3100–4590 ms frames include unfolding and two 420 ms cycles. All 13 source/art hashes match before/after; gate preceded integration. |
| Stable articulated anatomy | Near/far frames at matching phases, shoulder/joint contract checks, downstroke/recovery/body-response comparison; no miniature growth, panel rotation, duplicate closed wing or art jump | Study and integrated independent PASS. Moderate neck lean resolves the ribbon; near/far shoulders remain attached with distinct views and folding recovery. `verify-flight-body.mjs` checks width, bill proportions, stroke response and 3,465 occupied triangles, minimum area ratio 0.720. Compact engraving motion and brief edge-on profiles remain artistic limits. |
| Retained scene/wake/shake | Desktop/phone beginning at normal speed and close frames; engraved identity, connected motion and water, no background rectangles/page bleed-through | `integrated-review/` four normal-speed videos and sequence sheets accept the retained opening, connected departure and opaque setting; zero application page errors. |
| Feather and ripple | Adjacent quill/contact/ripple frames; contact origin/clock regression; quiet interval visible before pass one | Integrated sequence/native frames retain feather contact and the quiet ripple before the first passage; focused choreography/water contracts remain in the final source suite. |
| First left-to-right pass | Normal-speed complete traversal on desktop and phone; distinct beak/feet/wing/book silhouettes and trailing book entry | Integrated four-viewport PASS. First-pass adjacent frames show complete travel and trailing book entry; inspected cover, bill, feet and wing silhouettes stay distinct. |
| Exact book identity/data | Actual cover source and rendered crop; canonical title/author/publisher; test with changed live chapter statuses, including empty/noncontiguous statuses if the summary supports them | Focused book regression passes changed-status/empty/singular/noncontiguous fixtures. Actual cover and canonical title/Ben Labaschin/O’Reilly/update are inspected in `book-review/` and independently reconfirmed with accepted anatomy in `integrated-review/`. |
| Book quiet hold | Timestamped stable frames at both ends proving approximately 3–4 seconds; full-size 320/390 px reading check; direct copy and no temporary controls | `book-review/geometry.json` and integrated full recordings establish the stationary 10000–13300 ms hold. The 320/390 px cover and update are readable without temporary controls. |
| Second distinct left-to-right pass | Whole traversal on a visibly different path/depth; same book instance removal tied to passage; no premature reveal | Integrated PASS. Native second-pass frames show the smaller, higher left-to-right crossing taking the same card off the right edge; pure tangent regression passes. |
| Real-site reveal | Adjacent reveal frames and underlying DOM/content check; landscape remains opaque through hold, no duplicated book or transition seams | Integrated adjacent frames show the pond opaque through the hold and the actual site revealed with second-pass removal, without duplicate presentation. |
| Continuous bank/descent | Normal-speed second-pass-to-contact recording; boundary pose/velocity checks and adjacent bank frames; one identity/silhouette | Independent integrated PASS with bank/landing native frames and pure boundary/tangent checks. One identity follows the second crossing into the pond. |
| Water/resident handoff | Adjacent contact and handoff frames, exact resident target/alignment test, no crossfaded body/jump/post-contact scroll correction | Integrated PASS plus focused high-quality resting-body follow-up in `integrated-review/README.md`, `handoff-review.json` and source-paired video/frames. Outline and position match; minor detail change remains at the last fold before handoff. Requested and actual staged times differ in some frames, so use the JSON clock record rather than filenames alone. |
| Overall pacing | Final landmark table and normal-speed recording, roughly 16–18 seconds; one shared visible clock and stable hold | Implemented 17600 ms total and 10000–13300 ms stationary hold. Integrated recordings and final production normal playback preserve the single character/book/reveal clock. Unrecorded production phase records include all ten phases. |
| Mobile framing/readability | 320×568, 390×844 and 844×390 full intro; visible cover/update, complete usable landing/invitation and no horizontal overflow | Development independent PASS at 320×568, 390×844 and 844×390; final production Chromium phone and WebKit phone/short-landscape runs confirm readable framing and visible final action. Brief exiting-bird overlap with Skip at 320 px remains minor. Final native below-fold replay, bank rotation and Replay/Skip pass in `production-review/independent/results.json`. |
| Existing access lifecycle | Current preparation/hook/integration tests and browser cases in the table below | Production `lifecycle/results.json` records seven native cases with no application errors, removed transient content/locks and resumed access. `fallbacks.json` adds 11 cases; native history/preparation and exact source-contract mapping cover the remaining scopes. See interruption table. |
| Restrained pond behavior | Existing scheduler tests plus normal finish/bypass observations showing original max two roaming slots, immediate swimmer and one resident; dive/article behavior | Source Field cases preserve original styles/delays, one scheduler, two-slot cap and peeker. Native completion/fallback resumes ordinary swimmers. The independent final desktop review verifies pointer response, dive, article selection and actual link navigation; see `production-review/independent/`. |
| Bounded resources | Asset byte inventory, preparation counts, canvas/allocation caps, no hidden-frame draw work, reflection ownership and cleanup checks | Production maxima are 1,998,630 desktop and 1,199,968 phone-viewport pixels per character Canvas. Six readiness assets total 2,678,244 encoded bytes. Current factory/renderer tests verify cached materials/coverage, one preparation, shared reflection, no hidden drawing and failure/unmount disposal. Both performance runs end with no character Canvas. |
| Playback performance | Fresh unrecorded desktop and phone-viewport phase intervals, median/p95/max and setup long tasks; browser/host context and physical-device limitation | `production-review/performance.md` and JSON record fresh sequential unrecorded Chromium 147.0.7727.15 runs at 1440×900/DPR1 and 390×844/DPR3. All phase medians 8.3 ms; desktop launch p95 16.7 ms, other p95 values≤9.3 ms. Initial long tasks 128/126 ms remain a real startup limit. These are host RAF intervals, not physical-device or every-paint measurements. |
| Independent integrated critique | Named independent review with concrete findings, fixes and current rechecks; visual approval of anatomy and both passes | PASS in `integrated-review/README.md` at 1440×900, 390×844, 320×568 and 844×390, with 18 input hashes and separate handoff follow-up. Final production `independent/results.json` confirms Chromium desktop/phone, WebKit phone/landscape, native replay/rotation/Skip and pond interaction. First WebKit landscape wait timeout retained; fresh repeat passed. Minor 320 px Skip overlap and last-fold detail transition remain explicit. |
| Exact local production candidate | Source manifest/build ID; Node 22 source/type/build/served logs; verified preview manifest; final Chromium/WebKit evidence | PASS, current identity table and `release-record.json`. All 39 source regressions, types, production build, preservation of 910 files and served checks pass. Preview 3112 serves the exact retained build manifest; final native evidence uses this same build. |
| Local delivery/no publication | Current localhost:3112 working, evidence links/limitations delivered, baseline retained; no unauthorized push/merge/hosted preview/deploy/settings mutation | Verified candidate remains running at localhost:3112; prior snapshot retained. No push, merge, hosted preview, remote-setting mutation or deployment. The local preview, current recordings and concise REVIEW.md guide are delivered under task 5.6. |

## Interruption and failure cases

The records include final state as well as errors, covering transient content, focus/scroll, locks, Canvas cleanup, scheduler ownership and late restart. “Hook” and “Integration” line references use the exact file/hash mapping in `production-review/source-lifecycle-coverage.md`.

| Scenario | Concrete expected behavior | Current evidence |
| --- | --- | --- |
| Skip before hydration/preparation completes | Immediate usable site; late decoding cannot start an unsolicited film; original preparation deadline remains bounded | Native PASS in `production-review/native-preparation-history.json`: Skip with denied storage clears preparation before delayed framework scripts arrive; late scripts do not restart the film. Native/source deadline cases verify bounded fail-open and stale-load cancellation. |
| Skip during first pass/book entry | Book and bird disappear immediately; prior focus/scroll restored appropriately; no book timer survives | Native `lifecycle/results.json` first-pass-skip at 9017 ms removes dialog/book/locks, restores initial scroll, leaves body focus, and subsequent Tab reaches Skip to content. Later observation confirms no restart and one swimmer. |
| Escape during quiet hold | Same immediate exit as Skip, focus remains meaningful, no overlay/locks or delayed removal action | Native hold-escape at 11016 ms restores original scroll 0 from covered camera scroll 33, removes transient content/locks, resumes one swimmer; subsequent Tab reaches Skip to content. Source Integration 543–558 separately checks restoration to a focused replay trigger. |
| Skip during second pass/removal/bank | No partial presentation/reveal layer remains; ordinary pond and site usable | Native removal-skip at 14117 ms and bank-skip at 15108 ms pass. Both clear dialog/book/locks, restore initial scroll and usable keyboard navigation, resume one swimmer and do not restart later. |
| Replay from below the fold, then full finish | No unwanted anatomy/asset reinitialization midfilm; minimum camera travel; final full crest and visible invitation fit; completion does not scroll back | Native PASS in `production-review/independent/results.json`: Chromium replay begins at scroll 803 from #field-atlas, rotates 390×844→844×390 at 15051 ms and completes at scroll 549 with Find an article focused, invitation 317.05–355.05 below header 114.94. Source Integration 342–384 confirms persistent artwork/canvas ownership ; 513–520 and 425–459 cover camera/handoff contracts. |
| Replay from below the fold, then Skip | Restores the replay trigger's prior focus/scroll and releases all owned locks | Native PASS in the same independent combined case: second Replay/Skip restores scrollX 0, scrollY 1135 and Replay opening focus exactly, with overflow restored and no page errors. Source Integration 411–422/543–558 independently covers replay restoration and locks. |
| Hidden tab during book hold and second pass | Clock stops; book retains actual reading duration and character/reveal stay aligned after return | Source-only coverage in Hook 137–139 and Integration 387–408 freezes elapsed time/RAF and the book hold across 60000 ms, then resumes at its remaining deadline. Actual headed tab switching did not produce document.hidden in this runtime (`lifecycle/summary.json`); no visibility properties/events were manufactured. Native pause remains unverified, and second-crossing hidden pause is not separately staged. |
| Navigate/hash change/unmount during book hold | New route/anchor owns final scroll/focus; no stale work or page overlay remains | Native hold-navigation at 11017 ms reaches /book with no film/locks and returns home without replay. Source Hook 154–158/164 covers route/history cancellation; Integration 575–583 preserves destination anchor scroll/focus and 602–618 covers unmount cleanup. Standalone hold-specific unmount has source ownership coverage, not a separate native case. |
| Session repeat/history restoration/hash arrival | Automatic film eligibility remains unchanged; no new automatic replay or preparation flash | Native `native-preparation-history.json` confirms session reload and browser Back bypass the opening; a direct relationship anchor retains scroll 2779 with no opening. Hook 140/150/155–157 covers home-only soft Back and deliberate later entry. |
| Reduced motion before load and during film | Readable normal content; no automatic film/replay control under reduced motion; active ownership released when preference changes | Native initial case in `fallbacks.json` has no opening or visible Replay/swimmers. Native hold-reduced-motion at 11009 ms clears film/locks, restores scroll and hides Replay; turning motion back on resumes one swimmer without restarting the film. Hook 148–149 also verifies manual replay respects preference. |
| Missing/invalid new anatomy asset | Bounded recovery to normal site; no broken bird or locked cover and no late unsolicited start | Four native perspective-wing cases in `fallbacks.json` cover missing, malformed, explicitly rejected decode and response beyond deadline. All recover with readable content, no dialog/preparation/book/inert/canvas, one swimmer and no application errors. Source factory tests verify partial allocation and stale callbacks. |
| Missing/delayed/invalid book cover | No unreadable transient feature; bounded recovery; persistent text/book navigation still usable | Four native cover cases in `fallbacks.json` cover missing, malformed, explicitly rejected decode and response beyond deadline. All recover without transient book/locks/canvas, resume one swimmer and record no application errors. |
| JavaScript disabled/blocked; storage unavailable | Site remains readable and reachable; no permanent preparation layer; document fallback prevents late repeat where appropriate | Final native no-JavaScript and blocked-Next-bundle cases confirm readable unlocked content with no visible Replay, film or canvas, superseding old development control evidence. Native Skip with denied storage prevents late automatic starts; Hook 160–161 covers document-session fallback if storage is unavailable. |
| Resize during hold | Cover/update remain in bounds and readable; stage clock does not restart or truncate hold | Native hold-resize moves 390×844→844×390 at 10509→10967 ms; book remains in book phase and inside bounds (550×226.05, bottom 365.52). No horizontal overflow; Escape afterward clears locks and normal motion resumes. It does not restart the hold. |
| Portrait→844×390 rotation late in second pass/bank | Continuous retargeting, actual fixed-header clearance, visible bird/invitation; no snapping or unnecessary later correction | Native PASS in independent combined replay case: actual bank rotation at 15051 ms reaches a full landing at scroll 549 with visible invitation below the 114.94 px header. This supplies the native continuity result beyond source Integration 586–599, which allows graceful early exit. |
| Finish and then use pond | One resident; pointer/dive/article selection and link operate; no extra swimmer and original two-slot cadence resumes | Final normal playback restores Find an article focus and scrolling. Independent final desktop review observed pointer response, completed dive, selected article and successful article-link navigation. Source scheduler tests prove the unchanged two-slot cadence; see final independent artifacts. |

## Renderer review follow-up

`release-artifacts/2026-09-10-two-pass/renderer-review.md` records the partial-allocation leak, missing-image/surface failure reporting and unguarded child-study route, their fixes, current focused tests and the remaining integration checks. New wing/body factories release all five acquisition stages on context failure; real React checks verify once-only current fallback and inert callbacks after unmount. The subsequent independent study and integrated visual gates are recorded above. These component checks still do not establish final production behavior or frame performance.

## Accepted development evidence and scope

- `anatomy-critique/final-review.md` accepts the isolated study against its exact 13 source/art hash pairs, with desktop/phone normal-speed videos and native adjacent frames. The old thin-neck and clipping frames remain historical findings, not current blockers.
- `integrated-review/README.md` accepts the complete opening at four viewports. Eighteen source/art inputs are recorded in source-before/after; only the announced hydration-only replay control changed during the original capture. The subsequent resting-body sampling refinement has separate handoff source pairs, a normal-speed 844×390 video and bracketing frames.
- Normal completion restored scrolling and focused Find an article at scroll positions 0, 33, 310 and 549 px for desktop, 390 px, 320 px and short landscape respectively. These are browser-host observations, not physical-device or performance measurements.
- The handoff follow-up retains a minor detail change at the final wing fold; the 320 px exiting hero briefly passes behind the usable Skip button. The independent reviewer did not classify either as a blocker.
- Current focused rig/body, artwork/factory, reflection/renderer and lifecycle fallback suites pass under Node 22.14.0. The final complete source suite and production gate now pass, with exact candidate identity above.
- `functional-review/fallbacks-dev.json` and `native-book-escape-dev.json` support only the cases and state fields they actually record. The dead no-JavaScript replay element was fixed afterward by hydration-only rendering. Final post-fix production `fallbacks.json` supersedes the earlier dead-control observation and confirms no visible Replay without JavaScript.

## Production limits and final delivery

- Development anatomy and integrated visual gates passed against their recorded source/art hashes. Production build `QsI2QzDIQ1r87XsztmOSc` passes the complete 39-script gate and served identity checks; the final independent browser scope is retained under `production-review/independent/`.
- Six essential assets total 2,678,244 encoded bytes. Actual original/prompt/native-mask evidence remains in `artwork/`; `asset-inventory.json` records all deployed-source hashes. No external reference photography was reused as site artwork.
- The startup artwork work caused 128 ms desktop and 126 ms phone-viewport long tasks. Subsequent phase intervals were generally smooth on this host, but no zero-stutter, physical-phone or total graphics-memory claim is made.
- The last folded-wing material still has a minor detail change before handoff; at 320 px the departing hero briefly passes behind the usable Skip action. These were independently judged non-blocking. The compact quality of the moving engraving remains an artistic choice.
- Source tests cover native-preparation/controller hidden-time behavior. An actual headed tab-switch attempt did not produce document.hidden, so native pause remains unverified. The reviewer manufactured no visibility properties or events; this limit is explicit in lifecycle/summary.json.
- Local Vercel analytics may return 404/MIME console errors because its endpoint is unavailable on the standalone local server. Native checks distinguish those and aborted route-prefetch requests from application page errors; the recorded cases have no application page errors.
- Port 3112 serves the verified candidate; spare development 3113 is stopped. Final documentation is an evidence-only change after the retained source snapshot. The root recorded the documentation-only differences in production-review/final-source-comparison.json and delivered the preview under task 5.6. All reviewer browsers and their owned contexts are closed.
- Nothing was published. Publication approval remains separate and has not been granted for this change.

The final independent browser record preserves the first WebKit landscape wait timeout and the successful repeat instead of discarding the first attempt. Normal completion scroll positions were 0 for Chromium desktop, 33 for Chromium phone, 32 for WebKit phone and 549 for WebKit landscape. The final combined replay/rotation/Skip and real pointer/dive/article navigation passed with no application page errors. These are Chromium 147.0.7727.15 and WebKit 26.4 local browser-engine runs, not physical-device, Safari-application or hosted-production tests. The independent README and snapshot-inputs.json record the precise scope and 18 source/art hashes. Strict OpenSpec validation passes and local delivery is complete. Publishing remains subject to separate explicit approval.

## Subsequent user feedback on wing proportions

After local delivery, Ben reported that the wings still appear too small while opening. Earlier independent PASS results do not establish his artistic acceptance. Shareable MP4s of the current review draft are in `release-artifacts/2026-09-10-two-pass/share/`. A larger-wing comparison and reassessment are recorded in `wing-proportions/README.md`; the experiment was restored out of application source. The next anatomy refinement should address feather area, emergence timing and backward sweep together, then receive a fresh visual review. The current local preview remains unchanged and nothing was published.
