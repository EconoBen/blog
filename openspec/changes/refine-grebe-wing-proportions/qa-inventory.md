# Wing-proportion correction QA inventory

> Status — LOCAL VERIFICATION COMPLETE, September 10, 2026. Independent isolated and integrated visual gates passed. The final 43-suite production gate, four native Chromium/WebKit desktop/phone plays, scoped lifecycle checks, performance measurements and fresh MP4s are complete. Known source-only and device limits are retained below. Nothing was published; archive awaits the approved release workflow.

## Candidate and preserved baseline

All new evidence is under `release-artifacts/2026-09-10-wing-refinement/`. Paths below are relative to that evidence directory unless identified as source scripts.

| Field | Current local candidate |
| --- | --- |
| Preview | `http://localhost:3112/` |
| Build | `S-f02umZXzcU71qc9NRDg` |
| Source manifest | `49d6cece35ac4ac9bcd11cf9f24e7b1131e5117cb39d074a87a5cc7653064f71` |
| Retained source | `/var/folders/17/b7kp7zcd0xbc4vr9sm2w848h0000gn/T/econoben-release-pGdpjA/source` |
| Final gate | `final/release-record.json`, 922 source inputs |
| Served identity | `final/identity.json` and independent `final/independent/identity.json`; served build-manifest bytes match snapshot |
| Post-review source check | Parent identity records 618 application/public/script inputs unchanged; later planning/evidence updates are explicitly outside the frozen app/test/art candidate |
| Preserved rejected baseline | Build `QsI2QzDIQ1r87XsztmOSc`, snapshot `econoben-release-1Wilcd/source`, manifest `bb9cb299a1fa46e8aa5c98eb28900b8de26985dba6b75bc32c5b7208eb4d1be7` |
| Prior clips and functional evidence | `release-artifacts/2026-09-10-two-pass/share/` and `production-review/`; retained as history, not new-wing acceptance |

## Visual and production acceptance

| Requirement | Actual evidence and result |
| --- | --- |
| Concrete diagnosis and bounded alternatives | PASS. `references.md`, `study/README.md` and `study-review.md` retain genuine reference observations and rejected fuller/articulated geometry. After the focused follow-up failed, sectioned artwork addressed the pointed root, strip/hook and fragmented feather limitation. Earlier failures remain intact |
| Full-size early wings and connected anatomy | PASS. `sectioned-study-review-v2.md` records desktop/phone native comparisons and 29 staged poses per layout. Full-length segments, fuller early feather area, connected shoulder and clear head survive two strokes; no former recovery needle remains |
| Continuous material | PASS. Completed-wing alpha removes the 3110/3125 ms triangle facets. The isolated and integrated reviews inspect early replacement and closing. The brief initial crossfade remains disclosed rather than described as nonexistent |
| Independent gate before integration | PASS. Exact input hashes match in `sectioned-independent-v2/source-before.json` and `source-after.json`; selected sectioned profile was explicitly verified before native and staged captures. Both earlier failed reviews remain preserved |
| Meaningful source regressions | PASS. Final gate ran all 43 discovered suites. Current wing-unfolding, section preparation, actual Canvas material, rig/body, shared rendering, clearance and lifecycle contracts passed. Prior failing visual/clearance evidence is retained; numerical results alone did not close a visual gate |
| Complete film/body/book/rest | PASS. `integrated-independent-v2/README.md` records five complete native runs and 24 separately staged poses per viewport. The 17600 ms film, two book passes, 10000–13300 ms quiet hold, moderate neck/level bill and single resident handoff remain intact. Final desktop/phone production runs confirm the same story |
| Responsive geometry | PASS. Integrated development review covered 1440×900, 390×844, 320×568, 844×390 and 568×320. The universal 720–160 ms pre-contact closure prevents orientation-dependent wing reopening. The only post-review app change was the final compact-card width correction, checked below |
| Final compact landscape correction | PASS on production. At native 11042 ms and 568×320, card376×160.125 px, right edge472 px, Skip left483.703 px, gap11.703 px. Full title/cover/author/publisher/update and 44 px Skip remain readable and separate. `final/independent/chromium-short-landscape-final-gap.png` and `results.json` retain actual bounds |
| Current Chromium/WebKit film | PASS. `final/independent/README.md` and videos record native complete desktop 1440×900 and phone 390×844 in Chromium147.0.7727.15 and WebKit26.4. All finish with visible Find an article, pond focus and no horizontal overflow. No application page exceptions; local analytics404/MIME warnings retained |
| Frozen release gate | PASS. Locked dependencies, all 43 source suites, types, production build, unchanged 922-file snapshot, exact own-server build identity and six served checks passed in `final/release-record.json`. Rollback metadata retained; no deployment performed |
| Current shareable MP4s | PASS. `final/share/README.md` and `video-verification.json` identify full normal-speed recordings from this build. Desktop1440×900 is20.08s/4,488,095 bytes; phone 390×844 is20.04s/1,594,255 bytes. H.264/yuv420p25fps, faststart, no audio, full decoding and endpoint inspection passed; originals/encoding logs/hashes retained |
| Local delivery and cleanup | Ready. `final/README.md` supplies preview and clip links for the parent’s final response. Every review browser is closed and spare 3113 stopped; verified 3112 remains. No remote mutation, push, merge or publishing occurred |

## Lifecycle — source contracts versus native observations

Source references below name the current scripts run in the final 43-suite gate. Source fixtures exercise real component/hook logic under controlled timers or injected resources; they are not relabeled as native-browser observations.

| Contract | Current source support | Current native support and limits |
| --- | --- | --- |
| Selected artwork barrier, missing/delayed readiness and late callbacks | `verify-grebe-arrival-integration.mjs` around lines358–392 tests the selected v3 URL, deferred decode, missing resource, late completion and both scheduler extremes. `verify-grebe-arrival.mjs` lines144–146 bounds failed/stalled assets and prevents restart | Blocked v3 image restores the site; actual Replay then Skip succeeds in `final/lifecycle/results.json`. Missing/deferred/malformed or decode-rejected variants beyond this native case rely on the explicit source contracts that exercise them; no blanket native claim |
| Section allocation and scratch failure | `verify-wing-sections.mjs` exercises all 16 allocation-failure positions and actual Canvas registration/alpha/connected tips. `verify-flight-preparation.mjs` tests both profiles, body/wing/shoulder/main/scratch failure, throwing scratch context, once-only reporting, stale source switch and post-unmount callbacks | Interrupted native runs sample acquired buffers returning to 1×1. A seventh click probe arrived at3235 ms, after the short partial-alpha interval; exact interruption while scratch is active was not established natively and remains source-only |
| Skip/Escape and restored access | Integration around lines297–316/340–355/459–477/600–613 tests inert/scroll/focus, keyboard containment and exits; cleanup assertions run after each hook fixture | Six scoped final cases pass, including early/powered/closing Skip, book-hold Escape, unfolding reduced motion and missing-wing Replay/Skip. Elapsed values were read before screenshots/input and denote observed phase, not exact event time |
| Replay, late rotation and final invitation | Integration around lines504–673 tests camera clearance, below-fold replay, late orientation, one fold value at identical elapsed time and stable handoff | Independent native replay starts scroll 803, rotates390×844→568×320 at 15046 ms and finishes scroll 622 with visible action. Second Replay/Skip restores 1138 exactly and Replay focus. Final results/video retained |
| Reduced motion | Hook lines148–149 and integration reduced-motion cases test initial bypass, manual-replay respect, cancellation and no restart when preference returns | Final native unfolding preference change cancels and restores ordinary reduced-motion state. Other preference timing permutations remain source coverage |
| Session, storage, hash, navigation and history | Hook lines150–164 covers seen/blocked storage, hash/deep-scroll, pagehide/popstate, home-only persistent history marker and no replay away from home; preparation tests native-script markers/deadlines | `final/lifecycle/pond-and-navigation.json` confirms navigation to Book during replay at 3116 ms removes film/locks and browser Back does not restart. Wider storage/hash/prehydration cases remain current source tests plus explicitly historical native evidence |
| Hidden-time preservation | Hook hidden readiness and integration lines617–625 freeze composed elapsed time; `verify-arrival-preparation.mjs` hidden deadline case and `verify-grebe-field-lifecycle.mjs` hidden timer case pass | Not established natively in this revision. Prior real tab switch did not produce document.hidden. No synthetic event is claimed as a real background-tab observation |
| Resident and visitor behavior | `verify-grebe-field-lifecycle.mjs` protects two slots, opposite cadence, initial hold and immediate first visitor after exit; integration tests reuse and no late scheduler restart | Final native interruption states show visitors returning under ordinary rules. `final/lifecycle/pond-and-navigation.json` verifies visible article title, actual Read article link and matching destination heading; pointer/dive evidence is retained alongside it |

Preparation without hydration, resource timeout, failure suppression, reduced motion and document teardown remain exercised by `verify-arrival-preparation.mjs` and `verify-grebe-arrival.mjs`. No new claim is made that all older fallback scenarios were repeated natively on this build. Physical devices and the Safari application were not tested.

## Resource and performance evidence

`resource-inventory.md` records current hashes, source contours and allocation estimates. The generated plates are RGB images with baked backgrounds; Canvas masks supply exterior transparency while retaining enclosed white feathers. The selected wing adds 47,284 encoded bytes; the six unique opening preload assets total 2,725,528 bytes. Fifteen cropped wing views use 4,377,600 pixels, and all prepared character material is estimated at 32,782,912 RGBA bytes. These estimates exclude browser caches and GPU copies.

`verify-flight-rendering.mjs` around lines116–184 confirms the selected registry, fifteen cropped materials plus body/shoulder coverage, full-opacity triangle rendering with one completed-wing alpha application, one copied reflection, no hidden drawing or per-frame coverage reads, two-million-pixel character/scratch caps, scratch release at full opacity and all buffers disposed. Preparation failures and changing sources release old ownership. Pond buffer caps/cache cleanup remain in their current source regressions. `resource-inventory.md` is an independently reviewed source estimate, not a process-memory measurement.

Serial, unrecorded final production measurements ran after other reviewers/recorders closed their browsers. `final/performance/README.md`, `desktop.json` and `phone.json` retain real RAF callbacks, elapsed/phase, resources and long tasks. Desktop was 1440×900/DPR1; phone viewport 390×844/DPR3, Chromium147 on the Mac, localhost assets and no CPU throttling.

| Measure | Desktop | Phone viewport |
| --- | ---: | ---: |
| Typical RAF callback interval |8.3 ms|8.3 ms|
| Powered-flight p95 |17.5 ms|16.8 ms|
| Powered-flight maximum |18.5 ms|17.6 ms|
| Intervals over25 ms after initial rest |0|0|
| Startup long task |145 ms|141 ms|

Startup remains a roughly 140–145 ms interruption. Historical 128/126 ms values are comparison evidence, not proof of a controlled benchmark regression. Callback intervals do not directly measure GPU-presented frames; these runs do not establish physical-phone, remote-network or Safari/iOS performance. The25fps recordings establish visual continuity only.

## Retained limitations and completion boundary

The roughly 66 ms initial crossfade and stylized foreshortened inner fold remain visible under frame inspection. In very short landscape, Skip can overlap the still-inert invitation late in the film; the invitation is fully clear when it becomes the final action. Neither was judged blocking in this scoped independent review. Exact scratch-active interruption and native hidden-tab pause remain source-only observations; physical devices and Safari application remain untested. Local analytics404/MIME warnings are recorded without being presented as application crashes.

Current app/test/art source is frozen to the release snapshot. This final QA/design/tasks update and local review artifacts were written afterward and are expected documentation-only differences. `openspec validate refine-grebe-wing-proportions --strict --no-interactive` passed. Local implementation and evidence are complete; parent final delivery uses `final/README.md`. Publication approval and subsequent archive remain separate.
