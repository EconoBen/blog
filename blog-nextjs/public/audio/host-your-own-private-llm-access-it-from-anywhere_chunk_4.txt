Caddy is listening for requests on H T T P S and forwarding them to Ollama on the Synology (or whichever box you are using). Web UI. If you set up the Ollama Web UI and pointed a domain like ui.mydomain.com at it, open your browser: You should see a straightforward interface listing any models you have downloaded. From there, you can select a model and start chatting. If you hit connection errors, double-check: That you are on Tailscale and can ping your Pi. Caddy has a block in /etc/caddy/Caddyfile for ui.mydomain.com. Your Docker Compose for the WebUI maps the correct ports (3000:8080 or however you set it up). Testing the Chat Endpoint: If you are a command-line fan and want to chat via curl, try: With "stream": false, the response should arrive in one shot. If you would rather see each token appear in real time, set "stream": true and watch the output scroll by. In depth Ollama query via your CLI using Ollama. We get a successful response from the LLM! Add a Handy Alias: For extra convenience, you can add a quick alias in your ~/.bashrc (or ~/.zshrc) to query the LLM without retyping curl all the time: Then, just open a new terminal (or run source ~/.bashrc) and do: at "Knock knock" Response from the CLI A P I call to the hosted model. Voila! Instant local LLM request from your Tailscale-protected domain. *** Troubleshooting: **No Response or Timeouts**\ Confirm you can ping or curl :11434. If that fails, something is up with Tailscale or your Docker container. **SSL or Certificate Errors**\ Make sure your Cloudflare DNS challenge is set up correctly. Caddy automatically renews certs, but if your domain is new, it might take a few minutes to propagate. **Model OOM (Out of Memory)**\ If the model fails to load, consider picking a smaller one. Check your Synology’s actual RAM; a 7B+ model likely will not fit on a 4GB NAS. With this, you have proven your local LLM is not only up and running, but also accessible from anywhere you are connected via Tailscale—fully encrypted, no weird firewall rules, and no random open ports to the public internet. *** Keep an eye out for part two this series in which I demonstrate how to take your LLM to the next level and operate as your own, private knowledge system you can query with natural from anywhere in the world!