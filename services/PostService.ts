// Older server entry points (including search and the sitemap) share one source.
export { postService } from '../app/services/PostService';
export type { Post, TagCount } from '../app/services/PostService';
